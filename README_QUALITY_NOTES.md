# Quality notes

This is currently the strongest portfolio repository.

## Keep

- `pyproject.toml`
- `src/` package layout
- pytest configuration
- runtime artifact handling
- validation report generation
- baseline model comparison

## Improve next

- Add a short architecture diagram or pipeline section to the README.
- Add a `make test` or `just test` command.
- Add GitHub Actions if not already enabled.
- Add a small synthetic dataset fixture for fast CI tests.
- Add a model card explaining intended use, limitations, and dataset assumptions.
